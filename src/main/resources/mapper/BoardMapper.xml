<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.msa2024.boards.mapper.BoardMapper">

    <sql id="search">
        <where>
            <if test="searchKey != null and searchKey != ''">
                title like concat('%', #{searchKey}, '%')
            </if>
        </where>
    </sql>


    <select id="getList" resultType="com.msa2024.boards.BoardVO">

        SELECT * FROM tb_board b inner join tb_users u on b.userid = u.userid

        <include refid="search"/>
        order by boardid desc limit #{skip}, #{size}
    </select>

    <select id="getTotalCount" resultType="int">
        select
        count(*)
        from tb_board
        <include refid="search"/>
    </select>

    <select id="view" resultType="com.msa2024.boards.BoardVO">
        select
            b.*, m.name bwriter
        from tb_board b inner join tb_users m on b.userid = m.userid
        where boardid = #{boardid}
    </select>
    <update id="update" >
        update tb_board set
             title   = #{title},
            content = #{content}
        where boardid = #{boardid}
    </update>
    <update id="incViewCount" >
        update tb_board set
            viewCount = viewCount + 1
        where boardid = #{boardid}
    </update>

    <delete id="delete" >
        delete
        from tb_board
        where boardid = #{boardid}
    </delete>

    <delete id="allDelete" >
        delete
        from tb_board
    </delete>

    <insert id="insert" >
        insert into tb_board (
            title,
            content,
            userid
        ) values (
                     #{title},
                     #{content},
                     #{userid}
                 )
    </insert>

</mapper>
